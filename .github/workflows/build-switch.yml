name: Build GBAStation Switch Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    container:
      image: devkitpro/devkita64:latest
    env:
      RELEASE_REPO_TOKEN: ${{ secrets.RELEASE_REPO_TOKEN }}

    steps:
      - name: Check out frontend source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install Switch dependencies
        shell: bash
        run: dkp-pacman -S --noconfirm --needed switch-dev switch-curl

      - name: Build frontend
        shell: bash
        run: |
          chmod +x switchbuild.sh
          ./switchbuild.sh -j "$(nproc)"

      - name: Create release package
        id: prep
        shell: bash
        run: |
          set -euo pipefail
          TAG_NAME=${GITHUB_REF_NAME}
          VERSION_NAME=${TAG_NAME#v}
          PACKAGE_DIR=package
          ZIP_FILE=GBAStation.zip
          CHANGELOG_FILE="${VERSION_NAME}.txt"
          SUMS_FILE=SHA256SUMS.txt

          rm -rf "$PACKAGE_DIR"
          mkdir -p "$PACKAGE_DIR/switch"
          cp build_switch/GBAStation.nro "$PACKAGE_DIR/switch/GBAStation.nro"

          cmake -E chdir "$PACKAGE_DIR" cmake -E tar cf "../$ZIP_FILE" --format=zip switch
          (
            cd "$PACKAGE_DIR"
            sha256sum switch/GBAStation.nro > "../$SUMS_FILE"
          )
          sha256sum "$ZIP_FILE" >> "$SUMS_FILE"
          printf '%s\n' "$(git tag -l --format='%(contents)' "$TAG_NAME")" > "$CHANGELOG_FILE"

          echo "version_name=$VERSION_NAME" >> "$GITHUB_OUTPUT"
          echo "zip_file=$ZIP_FILE" >> "$GITHUB_OUTPUT"
          echo "changelog_file=$CHANGELOG_FILE" >> "$GITHUB_OUTPUT"
          echo "sums_file=$SUMS_FILE" >> "$GITHUB_OUTPUT"

      - name: Upload Nintendo Switch package
        uses: actions/upload-artifact@v4
        with:
          name: GBAStation-Switch-${{ github.sha }}
          path: |
            ${{ steps.prep.outputs.zip_file }}
            ${{ steps.prep.outputs.sums_file }}
            ${{ steps.prep.outputs.changelog_file }}
          if-no-files-found: error
          retention-days: 30

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          body_path: ${{ steps.prep.outputs.changelog_file }}
          files: |
            ${{ steps.prep.outputs.zip_file }}
            ${{ steps.prep.outputs.sums_file }}

      # Keep the historical release repository integration optional. It is not
      # part of the build dependency chain and must not fail the GitHub Release.
      - name: Clone legacy release repository
        if: env.RELEASE_REPO_TOKEN != ''
        shell: bash
        run: |
          git clone "https://x-access-token:${RELEASE_REPO_TOKEN}@github.com/beiklive/GBAStation_Release.git" release_repo
